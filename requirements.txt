# requirements.txt — transformer_thesis conda env
# Pin critico: transformers<4.49 (GPT-NeoX vmap bug)
# Generato con: pip freeze > requirements.txt + manual curation

transformers>=4.46,<4.49  # GPT-NeoX vmap/SDPA bug in >=4.49, see checkpoint_loop.py ENV-02
torch>=2.0.0
peft>=0.6.0
transformer-lens>=1.0.0
lm-eval>=0.4.0
bitsandbytes>=0.41.0
scikit-learn>=1.3.0
numpy>=1.24.0
pandas>=2.0.0
joblib>=1.3.0
tqdm>=4.65.0
pyyaml>=6.0
py-spy>=0.3.0